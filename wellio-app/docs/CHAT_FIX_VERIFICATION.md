# Agent 试用修复验收

## 2026-09-12 用户试用问题修复

用户在 3103 输入“你好”得到 `INVALID_MODEL_OUTPUT`；上一轮真实验收覆盖了业务问答，但没有覆盖问候，这是验收缺口。服务日志确认最终回复结构校验失败，原始模型输出没有留存，不能据此断言是哪一个字段的实际值导致失败。

- 输出契约明确 `markdown` 为正文，`trainingSummary` / `nutritionSummary` 可为字符串或显式 `null`。null 不更新 Today 卡片，不以问候覆盖已有有效摘要；空串、对象、数组和缺失字段仍拒绝。
- 可信只读意图下的纯问候/致谢，使用一次简短的真实模型请求与 JSON 输出模式，不开放模型工具、不生成建议。业务工具循环仍使用原生 tool calling，避免在业务每一步强制 JSON 输出。
- 必需上下文由 Node Runtime 直接调用原有认证 RPC 读取，不再让模型先请求一次读取；写入后仍重新读取，版本、授权、租约和幂等校验不变。历史数组不再随每轮当前上下文重复发送，按需使用 query_history。
- 前端缓存 Markdown 消息渲染；当同一错误已有消息卡片时，不在顶部和输入框重复显示。独立网络或上传错误仍显示。

验证：Python 254 项、Node 39 项、前端 99 项通过，类型检查和生产构建通过；完整受控 HTTP 链路的餐食修改、读后写校验、Undo、隔离、重放、断开取消和重启通过。

真实浏览器连续请求：“你好” 13.0 秒、第二次“你好” 1.8 秒、“谢谢” 2.8 秒、午餐事实问答 14.0 秒，均 complete。独立真实餐食半份修改 15.33 秒，随后显式 Undo 成功。所有调用仍使用 deepseek/deepseek-v4.1-flash。

浏览器按键测试共44次，输入到下一帧最长15.6毫秒，无超过50毫秒的长任务、无页面运行错误。这是当前桌面 Chrome 的小样本，不是跨设备性能保证。首次模型请求和复杂请求仍有等待；不能宣称所有问题均能秒回。Exa与图片本轮未重跑，沿用上一轮证据；RAG与安装包不在此验收范围。
